<h1>ユーザー一覧</h1>

<div class="mb-3">
  <table class="table table-hover">
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= link_to user.name, [:admin,user] %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
