<h1>ツイート一覧</h1>

<div class="mb-3">
  <table class="table table-hover">
    <tbody>
      <% @posts.each do |post| %>
        <tr>
          <td><%= link_to post.content, post %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
